@model Generics.DataModels.AdminModels.Project
@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <!-- Input -->
    <div class="row clearfix">
        <div class="col-lg-12 col-md-12 col-sm-12">
            <div class="card">
                <div class="body">
                    <h2 class="card-inside-title">Project Detail</h2>
                    <form method="post" name="Project-Form" enctype="multipart/form-data" asp-controller="Project" asp-action="Create">
                        <input type="hidden" name="CustomerId" value="@Model.CustomerId" />
                        <input type="hidden" name="ProjectId" value="@Model.ProjectId" />
                        <div class="row clearfix">
                            <div class="col-sm-6">
                                <label for="email_address">Name</label>
                                <div class="form-group">
                                    <input type="text" name="ProjectName" class="form-control" placeholder="Name" value="@Model.ProjectName" />
                                </div>
                            </div>
                            <div class="col-sm-12">
                                <div class="form-group form-check">
                                    <label class="form-check-label">
                                        <input class="form-check-input" asp-for="IsActive" /> @Html.DisplayNameFor(model => model.IsActive)
                                    </label>
                                </div>
                            </div>

                        </div>
                        <button type="submit" class="btn btn-raised btn-primary btn-round waves-effect">Add</button>
                    </form>

                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}