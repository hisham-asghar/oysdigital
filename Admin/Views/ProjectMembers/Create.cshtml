@model Generics.DataModels.AdminModels.ProjectMembers
@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <!-- Input -->
    <div class="row clearfix">
        <div class="col-lg-12 col-md-12 col-sm-12">
            <div class="card">
                <div class="body">
                    <h2 class="card-inside-title">Mobile Space Detail</h2>
                    <form method="post" name="ProjectMember_Form" enctype="multipart/form-data" asp-controller="ProjectMembers" asp-action="Create">
                        <input type="hidden" name="Id" value="@Model.Id" />
                        <input type="hidden" name="ProjectId" value="@Model.ProjectId" />
                        <div class="row clearfix">
                            @*<div class="col-sm-12">
                                <div class="form-group">
                                    <select asp-for="ProjectMemberTypesId" asp-items="ViewBag.ProjectMemberTypesId" class="form-control show-tick">
                                    </select>
                                </div>
                            </div>*@
                            <br />
                            <div class="col-sm-12">
                                <div class="form-group form-check">
                                    <label class="form-check-label">
                                        <input class="form-check-input" asp-for="IsActive" checked /> @Html.DisplayNameFor(model => model.IsActive)
                                    </label>
                                </div>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-raised btn-primary btn-round waves-effect">Add</button>
                    </form>

                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}